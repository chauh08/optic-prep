# GitHub personal access token with Models: Read permission.
# Used only by the server-side /api/generate route.
GITHUB_TOKEN=
# Optional. Must be a model available to your GitHub Models account.
GITHUB_MODELS_MODEL=openai/gpt-4.1-mini

